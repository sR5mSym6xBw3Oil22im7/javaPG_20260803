# Repository Instructions

このリポジトリで作業する Codex は、以下のルールを常に優先してください。

## 目的
- このプロジェクトの変更は、読みやすさと一貫性を保ちながら行う。
- 既存の挙動を不用意に変えず、必要最小限の変更にとどめる。

## コーディング規約
- 新しく追加するコード行には、直前の行に説明コメントを付ける。
- 既存コードを編集する場合も、変更した行の直前に説明コメントを維持する。
- コメントはその行で何をしているかを簡潔に書く。
- コメントのない新規コード行を追加しない。
- 例外を作らず、この規約を常に適用する。

## 変更方針
- 変更はできるだけ小さく、目的に直接関係する部分だけを直す。
- 既存の命名、構成、出力形式は、必要がない限り変えない。
- 文字コードや改行コードは、既存ファイルの形式に合わせる。

## 作業時の注意
- 破壊的な変更は避ける。
- 不明点があれば、推測で広く直すよりも、影響範囲を絞って確認する。
- 既存の未関連変更は触らない。

## 検証
- Java ファイルを変更したら、少なくとも `javac *.java` でコンパイル確認を行う。
- 挙動変更がある場合は、可能であれば実行確認も行う。
- コメント規約に違反する行が残っていないか、変更後に見直す。

## 返答の方針
- 変更内容は、何を直したか、なぜ直したか、必要なら次に何を確認すべきかの順で簡潔に説明する。
- ルールに従えない事情がある場合は、作業を続ける前に理由を明示する。
